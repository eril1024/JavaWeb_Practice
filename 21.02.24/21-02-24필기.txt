@ service에서 interface를 사용하는 이유?
 - 폴리멀티즘 적용을 위해

@ thymeleaf
 - $는 변수를 가지고옴 ex) 파라미터, 섹션 값 등
 - *를 통해서 선택적 변수를 가져옴
 - 선택자는 부모 자식 관계에서 사용 가능 

 - ${ } = 변수식
 - *{ } = 객체적의 변수식
 - #{ } = 메시지식
 - @{ } = 링크식 

@test
 - 통합 테스트는 컨트롤러 테스트로 생각하면됨
 - 프로젝트시 테스트 기록 남기기

@ 협업
 툴 : cvs, svn,  github
 -stage영역 필요한 이유 : 중간 메모리 역할, 
 rollback
