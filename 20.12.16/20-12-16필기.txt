@ checked에러는 try/catch로 반드시 예외처리를 해주어야한다.

@ main함수에서의 예외는 jvm이 받는다.

@ catch문을 여러개 사용시 부모 예외 클래스가 가장 나중에 와야한다.

@ finally 구문은 에러의 유무와 상관없이 무조건 실행된다.

@ try-with-resources : try()안에 구문을 사용함으로써 메모리를 관리해줌

@ 스택에는 함수가 쌓임 
 
@ 메모리 분류이유(스택,메서드,힙)
 -메모리를 아끼고 속도 몇에서 이득을 봄

@ object의 equals는 주소를 비교
