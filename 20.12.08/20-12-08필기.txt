프로그래밍시 하나의 함수당 기능한개가 좋음

마지막에는 기능을 한번에 실행할 수 있는 함수를 만들어 놓을 것

concat으로 합치면 새로운 String 객체를 따로 만듬

builder에서 print할때 tostring을 붙히는 이유?
println이 stringBuilder 객체를 받을 수 없음

배열은 메모리에서 연속된 공간을 잡는다.

객체생성시 인스턴스 변수에 기본적으로 초기화됨 (0과 null)으로

