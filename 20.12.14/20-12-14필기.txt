@ 오버라이딩 - 부모꺼를 호출해도 자식것이 나옴

@ 모든 클래스는 object클래스를 상속받음(자동으로 코드작성됨)

@ 디폴트 생성자

@ tostring의 원리

@ 인터페이스 안의 함수는 구현부분이 없다 = 함수선언이 없다.

@ 인터페이스안에서 구현부분이 없는 함수를 만들기 위해서
 abstract를 넣어야 하는데 안 넣어도 상관없다.(자동으로 들어가있음)
 아무것도 안넣은면 컴파일러가 public abstract를 넣어줌
 abstract = 자손이 구현하라

@ 인터페이스는 클래스가 아니다
 - 객체 생성 불가
 - 참조 변수로 선언은 가능

@ 인터페이스는 다중 상속 가능

@ 한개의 파일에서 public이 두개 있으면 안된다.

